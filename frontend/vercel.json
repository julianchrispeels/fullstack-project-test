{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://project-server-three.vercel.app/api/$1"
    }
  ],
  "build": {
    "src": "package.json",
    "use": "@vercel/static-build",
    "config": {
      "distDir": "build"
    }
  }
}
